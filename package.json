{
  "name": "Demo",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "express": "4.17.1",
    "express-validator": "^7.0.1",
    "nvm": "^0.0.4",
    "sequelize": "^6.23.0",
    "sqlite3": "^5.0.0"
  },
  "scripts": {
    "start": "npm run build --prefix=client && node ./src/server.js",
    "seed": "node ./scripts/seedDb.js",
    "install:both": "npm install --prefix=client && npm install"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  },
  "engines": {
    "node": ">=10.16.3"
  },
  "engineStrict": true
}
