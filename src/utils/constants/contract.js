const CONTRACT_STATUSES = {
    NEW: 'new',
    IN_PROGGRESS: 'in_progress',
    TERMINATED: 'terminated',
}

const PROFILE_TYPES = {
    CLIENT: 'client',
    CONTRACTOR: 'contractor'
}

module.exports = {
    CONTRACT_STATUSES,
    PROFILE_TYPES
}